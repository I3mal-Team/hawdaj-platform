# Hawdaj — Project Progress

**Last updated:** 2026-07-05
**Phase:** 2 — Feature Inventory & Deep Knowledge Base
**Mode:** Read-only analysis. No production code is modified. Only `AI/` files created/updated.

---

## Overall Progress

- **Total features identified:** 31
- **Features fully analyzed (100%):** 31 (01–31) 🏁 **COMPLETE**
- **Features in progress:** 0
- **Overall knowledge-base completion:** 100%

Progress bar: `[▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓] 100%`

---

## Per-Feature Status

| # | Feature | Priority | Status | % |
|----|---------|----------|--------|---|
| 01 | Authentication & Profile | P0 | ✅ Done | 100% |
| 02 | Home & Sliders | P0 | ✅ Done | 100% |
| 03 | Trips (v1 + v2) | P0 | ✅ Done | 100% |
| 04 | Places | P1 | ✅ Done | 100% |
| 05 | Stores | P1 | ✅ Done | 100% |
| 06 | Restaurants (Zad) | P1 | ✅ Done | 100% |
| 07 | Tasneef | P1 | ✅ Done | 100% |
| 08 | Exploration / Search | P1 | ✅ Done | 100% |
| 09 | Events | P1 | ✅ Done | 100% |
| 10 | Swalef & Stories | P1 | ✅ Done | 100% |
| 11 | Tour Guides | P1 | ✅ Done | 100% |
| 12 | Landmarks | P2 | ✅ Done | 100% |
| 13 | Favorites & Saved | P2 | ✅ Done | 100% |
| 14 | Rates & Reviews | P2 | ✅ Done | 100% |
| 15 | My Properties | P2 | ✅ Done | 100% |
| 16 | User Stories | P2 | ✅ Done | 100% |
| 17 | Notifications | P2 | ✅ Done | 100% |
| 18 | Taxonomy | P2 | ✅ Done | 100% |
| 19 | Splash & Onboarding | P3 | ✅ Done | 100% |
| 20 | Force Update | P3 | ✅ Done | 100% |
| 21 | Profile Settings | P3 | ✅ Done | 100% |
| 22 | Localization & RTL | P3 | ✅ Done | 100% |
| 23 | Media / Gallery / Uploads | P3 | ✅ Done | 100% |
| 24 | Networking / Interceptors / Env | P3 | ✅ Done | 100% |
| 25 | Real-time (WebSockets/Reverb) | P3 | ✅ Done | 100% |
| 26 | Dashboard / Admin | P2 | ✅ Done | 100% |
| 27 | Roles & Permissions | P2 | ✅ Done | 100% |
| 28 | CarModel Module (OUT OF SCOPE) | P4 | ✅ Done | 100% |
| 29 | Report Module (OUT OF SCOPE) | P4 | ✅ Done | 100% |
| 30 | Visitor Tracking / Guard | P4 | ✅ Done | 100% |
| 31 | Auto-Translation Observers | P3 | ✅ Done | 100% |

---

## Change Log

- **2026-07-05** — Phase 1 (architecture analysis) completed for both repos. Phase 2 started: created `AI/`, `FEATURE_INDEX.md`, `PROJECT_PROGRESS.md`, `FEATURES/`.
- **2026-07-05** — ✅ Feature 01 (Authentication & Profile) fully analyzed (100%). Created `FEATURES/01_Authentication.md` (31 sections + 7 KB sections). Added index files: FEATURE_GRAPH, SEARCH_INDEX, API_INDEX, DATABASE_INDEX.
- **2026-07-05** — ✅ Feature 02 (Home & Sliders / Discovery) fully analyzed (100%). Created `FEATURES/02_Home.md` (all 31 + 7 sections + full flow).
- **2026-07-05** — ✅ Feature 03 (Trips — flagship, v1+v2) fully analyzed (100%) via two parallel deep agents (Flutter + Laravel). Created `FEATURES/03_Trips.md` (31 + 7 sections, full v2 algorithm + flows).
- **2026-07-05** — ✅ Feature 04 (Places) fully analyzed (100%). Created `FEATURES/04_Places.md` (31 + 7 sections + full list/detail flow). Places = reference template for content entities (Stores/Zad/Events/Swalef/Guides).
- **2026-07-05** — ✅ Feature 05 (Stores) fully analyzed (100%). Created `FEATURES/05_Stores.md` (31 + 7 sections, focused on differences from Places: is_online, hierarchical CategoryOfStore, stats endpoint, near_places).
- **2026-07-05** — ✅ Feature 06 (Restaurants / Zad / ZadElgadel) fully analyzed (100%). Created `FEATURES/06_Restaurants.md` (31 + 7 sections; unique: menus, offers with discount window, food_categories pivot, menu_file PDF, client-side PDF export).
- **2026-07-05** — ✅ Feature 07 (Tasneef — unified listing hub / aggregator) fully analyzed (100%). Created `FEATURES/07_Tasneef.md` (31 + 7 sections; one cubit+repo+UnifiedPlaceModel serves 7 content types by switching entity index endpoints; unique `applications` type).
- **2026-07-05** — ✅ Feature 08 (Exploration — global search + interactive map) fully analyzed (100%). Created `FEATURES/08_Exploration.md` (31 + 7 sections; multi-model SearchController aggregation, circular map markers with LRU cache/batching, type-aware navigation).
- **2026-07-05** — ✅ Feature 09 (Events) fully analyzed (100%). Created `FEATURES/09_Events.md` (temporal date-window logic, UGC, broken social-link mappings noted).
- **2026-07-05** — ✅ Feature 10 (Swalef & Stories — heritage) fully analyzed (100%). Created `FEATURES/10_Swalef.md` (rich 9-field translated narrative schema, audio stored but no UI player, mainCharacters untranslated). First agent stalled/failed; retried successfully. User authorized continuing without per-feature approval (still output full report each).
- **2026-07-05** — ✅ Feature 11 (Tour Guides) fully analyzed (100%). Created `FEATURES/11_TourGuides.md` (31 + 7 sections; unique self-registration workflow, one-guide-per-user rule, top_rated AVG(rate) sorting, regions/languages multi-select JSON, separate photo upload endpoint).
- **2026-07-06** — ✅ Feature 12 (Landmarks) fully analyzed (100%). Created `FEATURES/12_Landmarks.md` (all 38 sections; lightweight UGC content entity — title/desc/address/type/single-image, stored active=0/pending). Flagged: index()+show() missing active filter (moderation bypass, HIGH), orphaned LandmarkShowCubit/detail flow, hardcoded placeholder rating, multi-image UI vs single-image API mismatch, no Translatable/Rate/Favorite unlike Places template.
- **2026-07-06** — ✅ Feature 13 (Favorites & Saved) fully analyzed (100%). Created `FEATURES/13_FavoritesSaved.md` (all 38 sections). TWO independent parallel toggle systems: Favorites (polymorphic morphMany Favorite) + Saved (separate `saves` table, NOT polymorphic). Flagged: N+1 on is_favorite/is_saved flags per list item (HIGH, 100 items ⇒ 201 queries), no unique constraint/index on either table, type-casing normalization fragility (ucwords + zad→Zad_elgadel), no content-existence/soft-delete check (orphaned rows + ghost "Resource not found" stubs), toggle-only (full list refetch). First agent killed by user; relaunched successfully.
- **2026-07-06** — ✅ Feature 14 (Rates & Reviews) fully analyzed (100%). Created `FEATURES/14_RatesReviews.md` (all 38 sections). Flat `rates` table (pseudo-polymorphic: parent_id + string type) serving 7 content types via `hasMany(Rate)->where('type',X)` + rate/review avg accessors. Flagged CRITICAL: POST+DELETE `rates` fully UNAUTHENTICATED (routes/api.php:147–148); reviewer email PII leaked in raw ratings array (PlaceResource:74 + all resources); no rate range 1–5 enforcement; no duplicate-rating prevention; self-rating + inactive-content rating allowed; N+1 avg accessors (Store re-queries via `ratings()`); rate stored as string; no index on parent_id/type.
- **2026-07-06** — ✅ Feature 15 (My Properties / My Submissions) fully analyzed (100%). Created `FEATURES/15_MyProperties.md` (all 38 sections). Hybrid architecture: unified hub `GET /my-properties` (PropertiesController@index aggregates Places/Stores/Events/Zads where added_by_user=1 & user_id=auth) + scattered per-type endpoints (myLandMarks, myStories, myLastDayStories, guide-own). Submission workflow: status='pending', active=0, ownership_proof_file required (properties only); Landmarks pending w/o proof/reason; Stories auto status='active'. Flagged: status enum casing mismatch (client 'accepted' vs backend pending/active), ownership_proof_file+rejected_reason exposed in LIST resources, no edit/delete-own API for properties (dashboard-only moderation), inconsistent moderation fields across types. Story delete ownership-checked ✅.
- **2026-07-06** — ✅ Feature 16 (User Stories — ephemeral last-24h) fully analyzed (100%). Created `FEATURES/16_UserStories.md` (all 38 sections). Half-built ephemeral system (distinct from Swalef F10). 24h enforced only by read-time `created_at >= now()->subDay()` query (StoryController:53). Flagged: NO expires_at column / cleanup cron (rows persist forever), NO seen/viewed tracking (no story_views table), Flutter list endpoint MIS-MAPPED to `get-profile-page` (returns full profile PII instead of stories; correct constant defined but unused), video mimes accepted but client renders images only, SVG upload allowed (stored-XSS risk), show() returns wrong LandMarkResource, no index on user_id/created_at, auto-approved status='active'. Delete IDOR-protected ✅.
- **2026-07-06** — ✅ Feature 17 (Notifications — FCM + Marketing) fully analyzed (100%). Created `FEATURES/17_Notifications.md` (all 38 sections). ASYMMETRIC: Laravel real (users.fcm_token, laravel-notification-channels/fcm + kreait/firebase-php HTTP v1, MarketingPushDispatcher→BroadcastMarketingNewContentJob chunk-150 on model activate). Flutter DISABLED: no firebase_messaging in pubspec; notification_manager/local_notification_service/reverb_websocket_service fully commented out; no token-registration code, no push handlers, no in-app list UI. Flagged: firebase service-account creds must be gitignored (HIGH verify); token not cleared on logout; no token uniqueness; no mobile list/mark-read API (dashboard-only); untargeted broadcast no opt-out; end-to-end push non-functional on device.
- **2026-07-06** — ✅ Feature 18 (Taxonomy — Regions/Cities/Categories/Prices/Languages) fully analyzed (100%). Created `FEATURES/18_Taxonomy.md` (all 38 sections). Shared translatable (Astrotomic AR/EN/RU/ZH) master data. Region→City + Category→SubCategory hierarchical (self-ref parent_id). Categories FRAGMENTED into 6 per-type models (Category/CategoryOfStore/CategoryOfZad/FoodCategoryOfZad/CategoryOfSwalef/CategoryOfApplication). All lookup endpoints public. Flagged: N+1 on translations (`Region::all()` loads all 4 locales/record, HIGH), Home uses hardcoded dummyCategories not API (staleness, HIGH), no client persistent cache (refetch every screen), `City::all()` unpaginated, 6-way category duplication, duplicate region cubits (core vs tourguide), no active filter on regions/cities/categories.
- **2026-07-06** — ✅ Feature 19 (Splash & Onboarding) fully analyzed (100%). Created `FEATURES/19_SplashOnboarding.md` (all 38 sections). Boot pipeline: ForceUpdateIntermediateScreen (Firebase Firestore app_versions) → SplashView (2s) → OnboardingView|HomeView by `seenOnboarding` pref. Onboarding = 3 hardcoded local-asset slides (no CMS/backend). NO dedicated bootstrap/config API — startup config smuggled inside `/api/home` (fired immediately at boot). Flagged: blocking network at splash (fetchHome/fetchSliders, no timeout/offline), seenOnboarding never resettable (survives reinstall, no replay), boot steps no try/catch (crash pre-UI), locale load races router init (wrong-language flash), force-update no offline fallback, default ar no first-run picker.
- **2026-07-06** — ✅ Feature 20 (Force Update) fully analyzed (100%). Created `FEATURES/20_ForceUpdate.md` (all 38 sections). Firebase Firestore ONLY (Laravel NOT involved, confirmed): collection `app_versions`, doc `latest_version`, fields {version, isMandatory}. Correct semver compare via `version` package (1.10>1.9 handled). Mandatory dialog truly non-bypassable (PopScope canPop:false + barrierDismissible:false); optional dismissable w/ ignored_update_version pref (optional-only). Flagged: FAIL-OPEN on Firebase error (mandatory gate bypassable network-wide during outage, no retry/backoff), build number ignored (no hotfix force), optional-ignore permanent, hardcoded store URLs.
- **2026-07-06** — ✅ Feature 21 (Profile Settings) fully analyzed (100%). Created `FEATURES/21_ProfileSettings.md` (all 38 sections). Account hub: edit profile, change-password (current-pw verified via Hash::check ✅), delete-account (soft delete + PARTIAL manual cascade), logout, language switch, get-profile-page aggregation (PersonalData+guide+social+myStories+myTrips, all PII). Flagged: contactus/send PUBLIC + no throttle (CRITICAL spam), account endpoints manual per-controller auth not route middleware (HIGH fragile), logout does NOT clear fcm_token (HIGH orphaned push — confirms F17 finding), delete cascade incomplete (Properties/Favorites/Saved/Rates orphaned), static pages (terms/about/faq) hardcoded in Flutter not CMS (translatable Setting model exists but no endpoint), contact form UI display-only (backend endpoint unused by client).
- **2026-07-06** — ✅ Feature 22 (Localization & RTL) fully analyzed (100%). Created `FEATURES/22_Localization.md` (all 38 sections). 4 locales ar/en/ru/zh both sides; ar default, en fallback. easy_localization + LocaleCubit (`selected_language` pref) + LanguageInterceptor (sends accept-language + locale headers). Flagged: backend ChangeLocale middleware reads `Locale`/`locale` header but IGNORES standard `Accept-Language` (works only because custom `locale` header also sent); RTL hardcoded to `languageCode=='ar'` (not script-based, won't handle future RTL locales); one `TextDirection.ltr` override in global_inner_pages_wrapper breaks RTL; 15+ hardcoded Arabic strings bypass i18n (onboarding 'استكشف', logout dialog, errors, dev screens); RU/ZH ~92% (Google-auto, ~40 keys short each); NO locale-aware date/number/currency (no Hijri, no SAR). EdgeInsetsDirectional used correctly ✅.
- **2026-07-06** — ✅ Feature 23 (Media/Gallery/Uploads) fully analyzed (100%). Created `FEATURES/23_MediaGallery.md` (all 38 sections). THREE coexisting backend media systems: Spatie MediaLibrary (singleFile, WebP conversions small 250×250/medium 500×500, nonQueued) + custom `galleries` table (parent_id+type, multi-image) + legacy `image` column, bridged by getMediaUrl/getImageUrl fallback chain. Flutter: FormDataHelper multipart + CachedNetworkImage (shimmer+logo fallback, SVG+AVIF). Flagged: server max:2048KB vs client 50MB contradiction, nonQueued conversions block upload response, SVG stored-XSS risk, public disks no CDN/auth on serve, no real client compression (picker constraints only), orphaned gallery files (no cascade), malformed-URL regex patch signals fragile URL build, no dimension validation.
- **2026-07-06** — ✅ Feature 24 (Networking/Interceptors/Env) fully analyzed (100%). Created `FEATURES/24_Networking.md` (all 38 sections). Dio layer, base URLs prod `dashboard.hawdaj.net/api/` vs test `test.dashboard.hawdaj.net/api/` (env in `environment` pref, default production). Interceptor chain: Language→Location(DEAD/commented)→Token(Bearer)→Chucker(opt)→PrettyDioLogger(always on). Token in secure storage. Flagged CRITICAL/HIGH: hardcoded `accept-tokenapi` key in dio_consumer.dart:22 (CRITICAL); env-switch dev BACKDOOR shipped in prod (hidden 5-tap + hardcoded `dev123`, switch to test API + Chucker, persisted, no build flavors) HIGH; PrettyDioLogger always-on logs tokens/PII HIGH; 401 detected but SILENTLY IGNORED (no refresh/logout, stale auth) HIGH; CORS allowed_origins ['*'] (credentials false); no explicit timeouts (30s default); no retry/backoff; envelope assumes message field.
- **2026-07-06** — ✅ Feature 25 (Real-time WebSockets/Reverb/Pusher/Agora) fully analyzed (100%). Created `FEATURES/25_Realtime.md` (all 38 sections). PLANNED/SCAFFOLDED, NOT WIRED. Flutter reverb_websocket_service.dart + notification_manager.dart 100% commented out; missing websocket_config.dart (broken import); no laravel_echo/pusher/web_socket_channel/agora deps. Laravel broadcasting registered but BROADCAST_DRIVER=null (no-op); one live event RealTimeMessage on PUBLIC events-{user_id} channel (car check-in, CarRequestDetailsController:99); CarEvent dead (never dispatched); Notifications DB-only (no ShouldBroadcast). CHANNEL MISMATCH: Flutter expects notification.patient.*/notification.parentinfo.*/private-conversation.* but channels.php only defines App.Models.User.{id}/events-*/cars.*. Chat + Agora video/voice = client placeholders w/ zero backend.
- **2026-07-06** — ✅ Feature 26 (Dashboard/Admin CRUD & Moderation) fully analyzed (100%). Created `FEATURES/26_DashboardAdmin.md` (all 38 sections). BACKEND-ONLY (web session/Blade, Flutter NOT involved). Per-entity Dashboard controllers + Spatie Permission (read/create/update/delete-{entity}) applied per-controller __construct (route group has NO global auth wrapper — fragile). Moderation: updateStatus (status pending/accepted/rejected + rejected_reason) + activate (active 0/1). Flagged CRITICAL/HIGH: **monarx-analyzer.php RCE artifact** (fetches+evals remote, self-deleting — DELETE immediately), Place `guarded=[]` mass-assignment (Store malformed), no group-level auth on dashboard routes, permission escalation (RolesController syncPermissions no check who can grant), no moderation audit trail, status vs active conflated (marketing push fires on active regardless of status), destroy_selected ids not integer-validated. CSRF enabled ✅. Web/API guard split clean ✅.
- **2026-07-06** — ✅ Feature 27 (Roles & Permissions — Spatie) fully analyzed (100%). Created `FEATURES/27_RolesPermissions.md` (all 38 sections). BACKEND-ONLY. 145 permissions (29 modules × 5 ops, `{op}-{module}`, guard=web only). Flagged CRITICAL/HIGH: mobile API has ZERO granular authorization (auth('api') only, any authenticated user hits any route — 0 permission:/role:/can() in Api controllers); ALL 7 roles (root/admin/customer/employee/guard/supervisor/contract_manager) get ALL 145 permissions → RBAC cosmetic; guard mismatch (perms web-only, unusable on api guard); hardcoded seeded creds `123456` for root/admin/etc; runtime permission grants no escalation check; 24h permission cache staleness; AuthServiceProvider empty (no Gate::before/policies). No implicit super-admin bypass ✅, no direct user→permission ✅.
- **2026-07-06** — ✅ Feature 28 (CarModel Module) fully analyzed (100%). Created `FEATURES/28_CarModule.md` (all 38 sections). **VERDICT: OUT OF SCOPE** for tourism/mobile. nwidart `Modules/CarModel/` = fleet/vehicle logistics + ANPR license-plate detection + guard check-in/out + daily risk analytics. Dashboard-only (Routes/api.php fully commented, zero Api/ car controllers, zero mobile touch). Shares only User model + guard/contract_manager roles. Entities: CarPlate/CarSetting/CarDay/CarNotes + app Car/CarRequest/Driver. Flagged: `cars.{siteId}` broadcast channel `fn()=>true` (no auth), takeAction permission middleware commented out, `car_requests` migration MISSING (fragile deploy), unrelated domain co-resident with tourism, no tests.
- **2026-07-06** — ✅ Feature 29 (Report Module) fully analyzed (100%). Created `FEATURES/29_ReportModule.md` (all 38 sections). **VERDICT: OUT OF SCOPE.** nwidart `Modules/Report/` = dashboard analytics + queued PDF(dompdf)/Excel(maatwebsite) export, reporting EXCLUSIVELY on CarModel ANPR data (F28) — no tourism content. `/api/report` is auth stub (no logic); no mobile exposure. Async export via cron `files:export` → ArchiveFile (status 0→1) → public disk. Properly auth+permission-gated (read-report/read-archive_file). Flagged (minor): exports on public disk (URL-guessable), no archive cleanup/TTL, OOM risk on huge exports (full result array), PII risk from free-text car_notes.
- **2026-07-06** — ✅ Feature 30 (Visitor Tracking / Guard Check-in / Task Activity) fully analyzed (100%). Created `FEATURES/30_VisitorTracking.md` (all 38 sections). TWO unrelated things: (A) mobile-facing content view tracking = REAL-BUT-BROKEN — `views_num` columns on Place/Store/Zad/Event exposed in resources + parsed by Flutter (not displayed), but NEVER incremented anywhere → topVisitedPlaces/topVisitedStores sort by constant 0 = FAKE popularity (id/random order); VisitSite logs home-page only (no user_id/content_id, random-IP fallback); MostVisit table + visit_item() helper DEAD/gutted. (B) Guard/Visitor/Contract/Material/Task = internal dashboard ops (PII-heavy: id_number/id_copy/personal_photo, health checks), OUT OF SCOPE, zero mobile exposure.
- **2026-07-06** — ✅ Feature 31 (Auto-Translation Observers — Google Translate) fully analyzed (100%). Created `FEATURES/31_TranslationObservers.md` (all 38 sections). ~11 observer classes synchronously scrape FREE Google Translate (stichoza/google-translate-php, unofficial, no API key) on model save: ar (authoritative) → en/ru/zh into {model}_translations. Flagged HIGH: SYNCHRONOUS BLOCKING (3–6s/save, external API in request cycle), ZERO error handling (Google outage/rate-limit → save FAILS all-or-nothing), free-tier scraping (breakable, IP-bannable ~100/day, no SLA), no caching (re-translate unchanged), bulk ops rate-limit/hang (1000 Places=6000 calls; migration backfill loops). Incomplete coverage: Swalef mainCharacters commented, GuideObserver Arabic commented, MenuObserver updated() empty, LanguageObserver disabled. Machine-translating heritage (Swalef) = quality risk.
- **2026-07-06** — 🏁 **KNOWLEDGE BASE COMPLETE: 31/31 features analyzed (100%).** All feature docs written under `AI/FEATURES/`. Index files current: FEATURE_INDEX, PROJECT_PROGRESS, FEATURE_GRAPH, SEARCH_INDEX, API_INDEX, DATABASE_INDEX.
- **2026-07-06** — 🌐 **Phase 3: Web Frontend integrated.** Analyzed Angular 16 SSR web app `/Users/mac/hawdaj-frontend` (read-only) and enriched the KB WITHOUT recreating docs. Created `WEB_FRONTEND.md` (architecture/folders/state/routing/API/auth/shared/build/env) + `WEB_MOBILE_COMPARISON.md` (matrix/missing/API-UI-business diffs/recommendations). Added web-only feature `FEATURES/32_AiAssistant.md` (ChatGPT on Places/Stores — mobile has none). Added a "Web Frontend" section to affected docs: 01,02,03,04,05,08,21,22,24. Updated FEATURE_INDEX (Web column + feature 32 + Web section). Key findings: web = Angular 16 SSR (NgUniversal), ngx-translate 4 locales + full SEO/hreflang/canonical, Firebase social→/social/callback, HttpClient interceptors (Bearer+locale+geo+api-key), **401→auto-logout (mobile ignores 401)**, granular home endpoints (8+ vs mobile's 1 /home), unified /global-search+map, PDF/print trip export, working contact+newsletter forms. Web implements ~22/31 shared; web-only feature #32; web lacks Splash/Onboarding, ForceUpdate, functional FCM, Realtime. Now 3 tiers documented: Mobile + API/Admin + Web.
- **2026-07-06** — 🏛️ **Phase 4: Governance layer built.** Created project constitution + full workflow/standards/testing/decisions structure so any session runs autonomously by trigger word. Added: `CLAUDE.md` (constitution, in AI/ + root loader pointer at repo root), `WORKFLOWS/` (MASTER_WORKFLOW + BUG_FIX + NEW_FEATURE + REFACTOR + CODE_REVIEW + PERFORMANCE + SECURITY — each an approval-gated pipeline triggered by short commands like "اعمل Bug"), `STANDARDS/` (ARCHITECTURE, CODING_STANDARDS, NAMING, FOLDER_STRUCTURE, STATE_MANAGEMENT, API_RULES, ERROR_HANDLING — codify actual patterns: Cubit/get_it/Either/go_router/DioConsumer/ApiModalController), `TESTING/` (REGRESSION_CHECKLIST, FEATURE_TESTS, SMOKE_TESTS), `DECISIONS/` (TECH_DEBT — 43 items D1–D43 severity-ranked + linked to features; ARCHITECTURE_DECISIONS — 12 observed ADRs + golden rule). KB now fully self-describing & self-governing.
- **2026-07-07** — 🔄 **KB sync policy + gap-fill.** Encoded the "docs always synced, updated without asking" policy into `CLAUDE.md §2` (mandatory context-load list + expanded step 11). Created the two referenced-but-missing files: `AI/CHANGE_LOG.md` (one line per change) + `AI/KNOWN_ISSUES.md` (live operational incidents K1–K4). Logged today's diagnostic findings: K1 prod SMTP 587 timeout → trips/save-trip-to-email 500 (synchronous mail, TripController.php:85); K2 APP_DEBUG=true in prod (stack-trace leak); K3 Spatie permission cache 24h never flushed (grants don't apply → permission:cache-reset); K4 user-create mail queued + silent catch. Added TECH_DEBT D44 (APP_DEBUG), D45 (sync mail no try/catch), D46 (perm cache). Wired CHANGE_LOG+KNOWN_ISSUES into MASTER_WORKFLOW/BUG_FIX step 8/11, FEATURE_INDEX, README, CLAUDE tree. Appended "Known Incidents (live)" to feature docs 03/26/27/21. Docs-only; no production code touched.
- **2026-07-07** — 📦 **KB migrated to standalone shared repo `/Users/mac/hawdaj-docs` (GitHub I3mal-Team/hawdaj-docs) = single source of truth for the whole ecosystem.** Each code repo now has a lightweight `CLAUDE.md` entry point (mobile/back/web) pointing to the shared repo and encoding: mandatory context-loading list, cross-repo awareness (API shared across mobile+web; golden rule Backend-first), and the doc-sync policy (task not complete until hawdaj-docs updated). Removed the duplicated nested `hawdaj-docs` clone from the mobile repo (clean/pushed/identical). Shared `CLAUDE.md §0` updated: standalone canonical status + cross-repo rule. No production code modified. **Manual step:** commit + push `hawdaj-docs` and the 3 lightweight `CLAUDE.md` files.
- **2026-07-07** — ⚖️ **Engineering governance strengthened: two mandatory policies.** Created `STANDARDS/Impact_Analysis.md` (mandatory before ANY implementation — 15 dimensions incl. Mobile/Backend/Web/DB/API/Shared Models/Auth/Permissions/Localization/Notifications/Real-time/Performance/Security/affected features/regression risks + Risk Level Low→Critical + Dependencies + Breaking Changes + Migration + Rollback + Testing scope, with a fill-in output template) and `STANDARDS/Completion_Gate.md` (mandatory 13-item completion checklist; any missing item ⇒ ❌ INCOMPLETE; docs-sync is non-optional). Integrated into `CLAUDE.md §2` (step 3 = Impact Analysis ref, new step 12 = Completion Gate), `MASTER_WORKFLOW` (pipeline now 12 steps + fixed-rules), and every execution workflow (BUG_FIX/NEW_FEATURE/REFACTOR/PERFORMANCE/SECURITY/CODE_REVIEW) via a "سياسات إلزامية" block referencing both docs (no duplication). Updated README golden rules + STANDARDS tree. Validation: every workflow now requires Impact Analysis before code and Completion Gate before done; no task completes without documentation sync. Docs-only (shared repo); no production code touched; no features re-analyzed.
- **2026-07-07** — 🔄 **Mandatory Documentation Synchronization Policy.** Created `STANDARDS/Documentation_Sync.md` declaring `hawdaj-docs` the Single Source of Truth that must never lag the code: decision flow (Code change → affects docs? → update hawdaj-docs → commit → push → complete), the exact Arabic stop-and-warn message, the update matrix by change type, and INCOMPLETE conditions (incl. **docs not committed/pushed**). Integrated into `CLAUDE.md §2` (policy + warning + step-12 commit/push), `Completion_Gate` (added hawdaj-docs committed + pushed items + warning), `MASTER_WORKFLOW` (fixed rule), all 7 workflows (governance block bullet), README (golden rule #5 + tree), CLAUDE tree. Enforces: no task completes while docs are outdated or unpushed. Docs-only; no production code touched; policy dog-fooded (this change itself committed+pushed to hawdaj-docs).
- **2026-07-07** — 🔎 **"Search-Before-Ask" policy.** Created `STANDARDS/Search_Before_Ask.md`: before ANY clarification question, search the KB for similar incidents (feature Common Bugs / Debug Guide / Known Incidents / KNOWN_ISSUES / TECH_DEBT / CHANGE_LOG / SEARCH_INDEX); if a match is found, present it **with a similarity %**; only if nothing is found, ask **one** targeted question. Wired into CLAUDE.md §2, MASTER_WORKFLOW, BUG_FIX step 1, README (golden rule #6 + tree). Docs-only; committed+pushed to hawdaj-docs.

- **2026-07-07** — 🛠️ **أول جولة تعديلات كود على فرع `add_ai` (موبايل) + مزامنة التوثيق.** التغييرات وأثرها بالتوثيق:
  - **[[03_Trips]] — حذف dead code v1:** بعد تأكيد أن فرع v1 غير قابل للتنفيذ (`AddTripView→useNew:true→FinishTrip→دايمًا kNewTripPlan`) حُذفت شاشات/cubits v1 المستقلّة (`TripPlan/MyTripView/MyTripDetailsView/TripPlanBody/TripDayRow/SaveTripView` + `TripPlanCubit/FinishTripDetailsCubit/SaveTripCubit/ViewTripCubit/MyTripCubit`) وroute keys `kTripPlan/kMyTripView/kMyTripDetailsView` (routes.dart −92 سطر). Models/repo methods خُلّيت (متشابكة مع wizard المشترك). `FinishTrip` شال باراميتر `tripModel`.
  - **[[03_Trips]] — reprepare (جديد، موبايل):** `ReprepareTripCubit` + `trip_repo.reprepareTrip` + `EndPoints.reprepareTrip` (`/v2/trips/reprepare`) موصولة بزر refresh في `NewTripPlanBody` عبر bottom-sheet تأكيدي (مفاتيح `reprepare_trip_confirm/warning` بالـ4 لغات ar/en/ru/zh).
  - **[[03_Trips]] — إصلاح daterange:** `PrepareTripWizardCubit` بدّل الترميز من end-start المربك إلى start-end الطبيعي (بناء + parsing)، ورفع مفتاح المسودّة `prepare_trip_draft_v1→_v2` لإبطال المسودّات القديمة.
  - **[[03_Trips]]/[[18_Taxonomy]] — تعريب أسماء المناطق:** `map_two.dart` `Country` أضاف `nameAr/nameRu/nameZh` + `localizedName(languageCode)` من سمات `name-ar/ru/zh` بـ`assets/images/map.svg`؛ كانت أسماء المناطق دايمًا إنجليزي (مصدرها SVG ثابت لا API taxonomy). مُستخدَمة بعرض الخريطة وتخزين الاسم عند الاختيار.
  - **[[02_Home]] — تنظيم widgets:** ~40 widget اتقسّمت من مجلد مسطّح لمجلدات فرعية بالمسؤولية (`app_bar/applications/explore_category/guides/offers/properties/sliders/suggestions/trip_card/shared`). حركة ملفات فقط + تحديث import paths بكل المستهلكين (places/events/restaurants/stores/stories/tourguide details bodies، see_all_ratings_list، إلخ). لا تغيير سلوك.
  - **[[01_Authentication]] — أول اختبارات بالمشروع:** `test/features/auth/` (social_auth_cubit_test 9 blocTest + social_auth_service_test 7 test) + `dev_deps` `bloc_test`/`mocktail`. انظر [[FEATURE_TESTS]].

---

## Global reference (established in Phase 1)

- **Flutter:** Feature-First + thin Clean Architecture · Cubit (flutter_bloc) · get_it · go_router · Dio · dartz Either · easy_localization (ar/en/ru/zh, RTL).
- **Laravel:** 8.12 · nwidart/laravel-modules · Sanctum (Bearer, non-expiring) · Spatie Permission + Astrotomic Translatable + Spatie MediaLibrary · MySQL · ApiModalController envelope `{status,message,data}`.
- **Base URLs:** prod `https://dashboard.hawdaj.net/api/` · test `https://test.dashboard.hawdaj.net/api/`.
- **Known CRITICAL security items:** `monarx-analyzer.php` (RCE), social login without ID-token verification, CORS `*`, committed secrets, missing `auth:api` on some user routes.
